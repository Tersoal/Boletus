{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Seccion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Categorias</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td><a href="{{ path('seccion_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>
                    <ul>
                        {% for categoria in entity.categorias %}
                            <li>
                                <a href="{{ path('categoria_show', { 'id': categoria.id }) }}">{{ categoria.name }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('seccion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('seccion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('seccion_new') }}">
                Create a new entry
            </a>
        </li>
        <li>
            <a href="{{ path('boletus_homepage') }}">
                Panel de Administración
            </a>
        </li>
    </ul>
    {% endblock %}
